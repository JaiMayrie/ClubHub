# Environment Configuration
NODE_ENV=development
PORT=5000

# Database Configuration (for local development without Docker)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=clubhub
DB_USER=postgres
DB_PASSWORD=your_password_here

# Note: When using Docker, these values are set in docker-compose.yml
# You don't need to create a .env file for Docker development